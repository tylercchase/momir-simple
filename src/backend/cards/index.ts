import type { IScryfallCard } from "../../../shared/cards/model";

export function getRandomCreature(
    manaCost: number,
    cards: IScryfallCard[],
): IScryfallCard | undefined {
    return undefined;
}
